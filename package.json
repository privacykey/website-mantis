{
  "name": "website-mantis",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs",
    "audit:browser": "node scripts/serve.mjs --audit",
    "sync:releases": "node scripts/sync-releases.mjs"
  },
  "devDependencies": {
    "axe-core": "4.13.0"
  }
}
